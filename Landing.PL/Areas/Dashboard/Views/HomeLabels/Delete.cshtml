@model HLebelVM

@section Header {
    Delete Home Label
}

<div class="col-md-12">

    <div class="card card-danger ">
        <div class="card-header">
            <h3 class="card-title">Delete Confirmation @Model.Name</h3>
        </div>
        <!-- /.card-header -->
        <!-- form start -->
        <div class="card-body">
            <p>Are You Sure of Delete @Model.Name Home Label ?</p>

        </div>
        <div class="card-footer">
            <form asp-controller="HomeLabels" asp-action="DeleteConfirm" asp-route-id="@Model.Id">
                @*  <input type="hidden" asp-for="Id" />  *@
                <button type="submit" class="btn btn-danger">Delete</button>
                <a asp-controller="Services" asp-action="Index" class="btn btn-secondary">Cancel</a>
            </form>

        </div>
    </div>
</div>